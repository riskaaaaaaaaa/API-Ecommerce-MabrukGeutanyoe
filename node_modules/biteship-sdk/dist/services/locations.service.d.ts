import { HttpClient } from '../http/client';
import { CreateLocationRequest, UpdateLocationRequest, LocationResponse, UpdateLocationResponse } from '../types';
export declare class LocationsService {
    private http;
    constructor(http: HttpClient);
    /**
     * Create a new location
     * @param request - Location creation data
     * @returns Promise with location response
     */
    create(request: CreateLocationRequest): Promise<LocationResponse>;
    /**
     * Get a location by ID
     * @param id - Location ID
     * @returns Promise with location response
     */
    get(id: string): Promise<LocationResponse>;
    /**
     * Update a location
     * @param id - Location ID
     * @param request - Location update data
     * @returns Promise with update response
     */
    update(id: string, request: UpdateLocationRequest): Promise<UpdateLocationResponse>;
    /**
     * Delete a location
     * @param id - Location ID
     * @returns Promise with response
     */
    delete(id: string): Promise<{
        success: boolean;
    }>;
}
