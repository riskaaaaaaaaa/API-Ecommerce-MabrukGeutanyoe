import { HttpClient } from '../http/client';
import { CreateDraftOrderRequest, UpdateDraftOrderRequest, DraftOrderResponse, RatesResponse } from '../types';
export declare class DraftOrdersService {
    private http;
    constructor(http: HttpClient);
    /**
     * Create a draft order
     * @param request - Draft order creation data
     * @returns Promise with draft order response
     */
    create(request: CreateDraftOrderRequest): Promise<DraftOrderResponse>;
    /**
     * Get a draft order by ID
     * @param id - Draft order ID
     * @returns Promise with draft order response
     */
    get(id: string): Promise<DraftOrderResponse>;
    /**
     * Update a draft order
     * @param id - Draft order ID
     * @param request - Draft order update data
     * @returns Promise with draft order response
     */
    update(id: string, request: UpdateDraftOrderRequest): Promise<DraftOrderResponse>;
    /**
     * Delete a draft order
     * @param id - Draft order ID
     * @returns Promise with response
     */
    delete(id: string): Promise<{
        success: boolean;
    }>;
    /**
     * Confirm a draft order (convert to order)
     * @param id - Draft order ID
     * @returns Promise with order response
     */
    confirm(id: string): Promise<DraftOrderResponse>;
    /**
     * Get rates for a draft order
     * @param id - Draft order ID
     * @returns Promise with rates response
     */
    getRates(id: string): Promise<RatesResponse>;
}
